---
title: Running Siriusec on GCP
description: How to install and configure Siriusec Siriusec on GCP for SSH and Kubernetes access.
---

We've created this guide to give customers a high level overview of how to use Siriusec
on [Google Cloud](https://cloud.google.com/gcp/) (GCP). This guide provides a high level
introduction leading to a deep dive into how to setup and run Siriusec in production.

We have split this guide into:

- [Siriusec on GCP FAQ](#siriusec-on-gcp-faq)
- [GCP Siriusec Introduction](#gcp-siriusec-introduction)
- [GCP Quickstart](#gcp-quickstart)

## Siriusec on GCP FAQ

### Why would you want to use Siriusec with GCP?

As leader in [BeyondCorp](https://cloud.google.com/beyondcorp/), GCP already provides
some great tools out of the box such as [Cloud Identity-Aware Proxy](https://cloud.google.com/iap/).
This is an excellent tool to quickly get setup securely with GCP but it can become
complicated to integrate into existing workflows and complicated if you want to share
tool across clouds.

### Which Services can I use Siriusec with?

You can use Siriusec for all the services that you would SSH into. This guide is
focused on Google Compute Engine. 

We plan on expanding our guide to eventually include using Siriusec with Google Kubernetes Engine (GKE).

## GCP Siriusec Introduction

This guide will cover how to setup, configure and run Siriusec on GCP.

GCP Services required to run Siriusec in High Availability:

- [Compute Engine: VM Instances with Instance Groups](#compute-engine-vm-instances-with-instance-groups)
- [Computer Engine: Health Checks](#computer-engine-health-checks)
- [Storage: Cloud Firestore](#storage-google-cloud-storage)
- [Storage: Google Cloud Storage](#storage-google-cloud-storage)
- [Network Services: Load Balancing](#network-services-load-balancing)
- [Network Services: Cloud DNS](#network-services-cloud-dns)

Other things needed:

- [SSL Certificate](https://cloud.google.com/load-balancing/docs/ssl-certificates)

Optional:

- Management Tools: Cloud Deployment Manager
- Stackdriver Logging

We recommend setting up Siriusec in High Availability mode. In High Availability mode Firestore 
stores the state of the system and Google Cloud Storage stores the audit logs.

![GCP Intro Image](../../../img/gcp/gcp-siriusec.svg)

### Compute Engine: VM Instances with Instance Groups

To run Siriusec in a High Availability configuration we recommend using `n1-standard-2` instances in
Production. It's best practice to separate the proxy and authentication server, using
Instance groups for the proxy and auth server.

### Computer Engine: Health Checks

GCP relies heavily on [Health Checks](https://cloud.google.com/load-balancing/docs/health-checks),
this is helpful when adding new instances to an instance group.

To enable health checks in Siriusec start with `siriusec start --diag-addr=0.0.0.0:3000`
see  [Admin Guide: Troubleshooting](../admin/troubleshooting.mdx) for more information.

### Storage: Cloud Firestore

[Cloud Firestore](https://cloud.google.com/firestore/) This storage backend uses real-time
updates to keep individual auth instances in sync and requires Firestore configured
in native mode.

Add this storage configuration in siriusec section of the config file (by default it's `/etc/siriusec.yaml`):

```yaml
siriusec:
  storage:
    type: firestore
    collection_name: cluster-data
    project_id: EXAMPLE_gcp-proj-with-firestore-enabled
    credentials_path: /var/lib/siriusec/firestore_creds.json
    audit_events_uri: ['firestore://events_table_name?projectID=example_Siriusec-Project-Name&credentialsPath=/var/lib/siriusec/gcs_creds.json']
```

### Storage: Google Cloud Storage

When creating the Bucket we would recommend setting it up as `Dual-region` and with
`Standard` storage class. Provide access using a `Uniform` access control with a Google-managed
key.

When setting up `audit_sessions_uri` use `gs://` session prefix.

```yaml
storage:
    ...
    audit_sessions_uri: 'gs://siriusec-session-storage-2?projectID=EXAMPLE_gcp-proj-with-firestore-enabled&credentialsPath=/var/lib/siriusec/gcs_creds.json'
    ...
```

### Network Services: Load Balancing

Load Balancing is required for Proxy and SSH traffic. Use `TCP Load Balancing` as
Siriusec requires custom ports for SSH and Web Traffic.

### Network Services: Cloud DNS

Cloud DNS is used to setup the public URL of the Siriusec Proxy. Once setup an `A`
record is sufficient.

### Access: Service accounts

The Authentication server will need to read and write to Firestore.  For this it'll need
the correct permission via Server Accounts. Learn how to [enable and create service accounts for instances](https://cloud.google.com/compute/docs/access/create-enable-service-accounts-for-instances).

![Service Account](../../../img/gcp/gcp-permissions.png)

**Download JSON Service Key**
![GCP Service Key](../../../img/gcp/gcp-service-key.png)

## GCP Quickstart

## 1. Create Resources

We recommend starting by creating the resources. We highly recommend creating these
an infrastructure automation tool such as [Cloud Deployment Manager](https://cloud.google.com/deployment-manager/) or Terraform.

## 2. Install & Configure Siriusec

Follow install instructions from our [installation page](../../installation.mdx#linux).

We recommend configuring Siriusec as per the below steps:

**1. Configure Siriusec Auth Server** using the below example `siriusec.yaml`, and start it
using [systemd](https://raw.githubusercontent.com/siriusec/siriusec/master/examples/systemd/siriusec.service) or use DEB/.RPM packages available from our [Downloads Page](https://siriusec.com/siriusec/download/).

```yaml
#
# Sample Siriusec configuration siriusec.yaml file for Auth Server
#
siriusec:
  nodename: siriusec-auth-server
  data_dir: /var/lib/siriusec
  pid_file: /run/siriusec.pid
  auth_token: EXAMPLE-CLUSTER-JOIN-TOKEN
  auth_servers:
  - 0.0.0.0:3025
  connection_limits:
    max_connections: 15000
    max_users: 250
  log:
    output: stderr
    severity: DEBUG
  storage:
    type: firestore
    collection_name: cluster-data
    # Credentials: Path to google service account file, used for Firestore and Google Storage.
    credentials_path: /var/lib/siriusec/gcs_creds.json
    project_id: example_Siriusec-Project-Name
    audit_events_uri: 'firestore://events?projectID=example_Siriusec-Project-Name&credentialsPath=/var/lib/siriusec/gcs_creds.json'
    audit_sessions_uri: 'gs://siriusec-session-storage-2?projectID=example_Siriusec-Project-Name&credentialsPath=/var/lib/siriusec/gcs_creds.json'
auth_service:
  enabled: yes
  auth_service:
    tokens:
    - "proxy,node:EXAMPLE-CLUSTER-JOIN-TOKEN"
```

**2. Setup Proxy**

Save the following configuration file as `/etc/siriusec.yaml` on the Proxy Server:

```yaml
siriusec:
  auth_token: EXAMPLE-CLUSTER-JOIN-TOKEN
  # We recommend using a TCP load balancer pointed to the auth servers when
  # setting up in High Availability mode.
  auth_servers: [ "auth.example.com:3025" ]
  # enable ssh service and disable auth and proxy:
ssh_service:
  enabled: true
auth_service:
  enabled: false
proxy_service:
  enabled: false
```

**3. Setup Siriusec Nodes**

Save the following configuration file as `/etc/siriusec.yaml` on the node:

```yaml
siriusec:
  auth_token: EXAMPLE-CLUSTER-JOIN-TOKEN
  # We recommend using a TCP load balancer pointed to the auth servers when
  # setting up in High Availability mode.
  auth_servers: [ "auth.example.com:3025" ]
  # enable ssh service and disable auth and proxy:
ssh_service:
  enabled: true
auth_service:
  enabled: false
proxy_service:
  enabled: false
```

**4. Add Users**

Follow [adding users](../../enterprise/getting-started.mdx#adding-users) or integrate with [Google Workspace](../../enterprise/sso/google-workspace.mdx) to provide SSO access.
