---
title: Kubernetes Access from standalone Siriusec
description: Connecting standalone Siriusec installations to Kubernetes clusters.
---

## Standalone Siriusec installation

Siriusec can connect to external Kubernetes clusters without using the Siriusec Helm chart or running a pod inside of the Kubernetes cluster. To do this, Siriusec needs a `kubeconfig` file to authenticate against the Kubernetes API.

(!docs/pages/includes/permission-warning.mdx!)

## Generating kubeconfig

First, configure your local `kubectl` command to point at the Kubernetes
cluster you want to register. You can use `kubectl config get-contexts` to
verify that the correct cluster is selected, or `kubectl config use-context
${CONTEXT_NAME?}` to switch to cluster `CONTEXT_NAME`.

Next, use [get-kubeconfig.sh](https://github.com/siriusec/siriusec/blob/master/examples/k8s-auth/get-kubeconfig.sh) to create a `kubeconfig` for Siriusec to use.

You can connect multiple Kubernetes clusters to Siriusec from one `kubeconfig` if it contains multiple entries. Use
[merge-kubeconfigs.sh](https://github.com/siriusec/siriusec/blob/master/examples/k8s-auth/merge-kubeconfigs.sh) to combine multiple `kubeconfigs` generated by `get-kubeconfig.sh`.

## Adding kubeconfig to Siriusec

In your Siriusec Proxy or a new separate instance, add the following to
`siriusec.yaml`:

```yaml
# ...
proxy_service:
  # ...
  public_addr: proxy.example.com:3080

  kube_listen_addr: 0.0.0.0:3026
  # optional: set a different public address for kubernetes access
  kube_public_addr: kube.example.com:3026

kubernetes_service:
  enabled: yes
  listen_addr: 0.0.0.0:3027
  # replace this path with the actual path for your generated kubeconfig
  kubeconfig_file: "path/to/kubeconfig"
```

<Admonition type="warning" title="Warning" >
  When using `kubeconfig_file`, EKS users may need to replace illegal characters in the `context` names.
  Supported characters are alphanumeric characters, `.`, `_`, and `-`. EKS typically includes `:` and `@`
  in their `kubeconfig` which are not allowed in Siriusec.
</Admonition>

After Siriusec starts with the above config, you should be able to see all new clusters using:

```code
$ tsh kube ls
# or
$ sudo tctl get kube_service
```
