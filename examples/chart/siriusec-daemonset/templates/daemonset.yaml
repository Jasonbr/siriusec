apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ template "siriusec.fullname" . }}-node
  labels:
{{ include "siriusec.labels" . | indent 4 }}
{{- if .Values.labels }}
{{ toYaml .Values.labels | indent 4 }}
{{- end }}
spec:
  selector:
    matchLabels:
      app: {{ template "siriusec.name" . }}-node
  template:
    metadata:
      labels:
        app: {{ template "siriusec.name" . }}-node
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
      annotations:
        checksum/config: {{ toYaml .Values.config | sha256sum }}
{{- if .Values.daemonset.Annotations }}
{{ toYaml .Values.daemonset.Annotations | indent 8 }}
{{- end }}
    spec:
      {{- if .Values.daemonset.runOnMasters }}
      tolerations:
      - key: node-role.kubernetes.io/master
        operator: "Exists"
        effect: "NoSchedule"
    {{- end }}
      hostPID: true
      containers:
      - name: {{ .Chart.Name }}-systemd-installer
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command: ["/usr/bin/dumb-init"]
        args: ["/bin/bash", "/usr/local/bin/siriusec-scripts/install-siriusec-systemd.sh"]
        env:
        {{- range $key, $value := .Values.daemonset.extraVars }}
        - name: {{ $key }}
          value: {{ $value }}
        {{- end}}
        resources:
{{ toYaml .Values.resources | indent 10 }}
        volumeMounts:
{{- if .Values.license.enabled }}
        - mountPath: {{ .Values.license.mountPath }}
          name: {{ template "siriusec.fullname" . }}-license
          readOnly: true
{{- end }}
        - mountPath: /etc/siriusec
          name: {{ template "siriusec.fullname" . }}-daemonset-config
          readOnly: true
        - mountPath: /usr/local/bin/siriusec-scripts
          name: {{ template "siriusec.fullname" . }}-bootstrap-scripts
          readOnly: true
        - mountPath: /host/etc/siriusec
          name: {{ template "siriusec.fullname" . }}-daemonset-host-config
        - mountPath: /etc/systemd/system
          name: {{ template "siriusec.fullname" . }}-daemonset-host-etc-systemd-system
        - mountPath: /run
          name: {{ template "siriusec.fullname" . }}-daemonset-host-run
        - mountPath: /host/var/lib/siriusec
          name: {{ template "siriusec.fullname" . }}-daemonset-host-storage
        - mountPath: /sys/fs/cgroup
          name: {{ template "siriusec.fullname" . }}-daemonset-host-sys-fs-cgroup
          readOnly: true
        - mountPath: /sys/fs/cgroup/systemd
          name: {{ template "siriusec.fullname" . }}-daemonset-host-sys-fs-cgroup-systemd
        - mountPath: /host/usr/local/bin
          name: {{ template "siriusec.fullname" . }}-daemonset-host-usr-local-bin
        - mountPath: /var/log/siriusec.log
          name: {{ template "siriusec.fullname" . }}-daemonset-host-var-log-siriuseclog
          readOnly: true
        - mountPath: /var/run/dbus
          name: {{ template "siriusec.fullname" . }}-daemonset-host-var-run-dbus
{{- if .Values.daemonset.extraVolumeMounts }}
{{ toYaml .Values.daemonset.extraVolumeMounts | indent 8 }}
{{- end }}
      volumes:
{{- if .Values.license.enabled }}
      - name: {{ template "siriusec.fullname" . }}-license
        secret:
          secretName: {{ .Values.license.secretName }}
{{- end }}
      - name: {{ template "siriusec.fullname" . }}-bootstrap-scripts
        configMap:
          name: {{ template "siriusec.fullname" . }}-bootstrap-scripts
      - name: {{ template "siriusec.fullname" . }}-daemonset-config
        configMap:
          name: {{ template "siriusec.fullname" . }}-daemonset-config
      - name: {{ template "siriusec.fullname" . }}-daemonset-host-etc-systemd-system
        hostPath:
          path: /etc/systemd/system
          type: Directory
      - name: {{ template "siriusec.fullname" . }}-daemonset-host-config
        hostPath:
          path: /etc/siriusec
          type: DirectoryOrCreate
      - name: {{ template "siriusec.fullname" . }}-daemonset-host-run
        hostPath:
          path: /run
          type: Directory
      - name: {{ template "siriusec.fullname" . }}-daemonset-host-sys-fs-cgroup
        hostPath:
          path: /sys/fs/cgroup
          type: Directory
      - name: {{ template "siriusec.fullname" . }}-daemonset-host-sys-fs-cgroup-systemd
        hostPath:
          path: /sys/fs/cgroup/systemd
          type: Directory
      - name: {{ template "siriusec.fullname" . }}-daemonset-host-usr-local-bin
        hostPath:
          path: /usr/local/bin
          type: DirectoryOrCreate
      - name: {{ template "siriusec.fullname" . }}-daemonset-host-storage
        hostPath:
          path: /var/lib/siriusec
          type: DirectoryOrCreate
      - name: {{ template "siriusec.fullname" . }}-daemonset-host-var-log-siriuseclog
        hostPath:
          path: /var/log/siriusec.log
          type: FileOrCreate
      - name: {{ template "siriusec.fullname" . }}-daemonset-host-var-run-dbus
        hostPath:
          path: /var/run/dbus
{{- if .Values.extraVolumes }}
{{ toYaml .Values.extraVolumes | indent 6 }}
{{- end }}
{{- if .Values.image.pullSecrets }}
      imagePullSecrets:
{{ toYaml .Values.image.pullSecrets | indent 6 }}
{{- end }}
{{- if .Values.affinity }}
      affinity:
{{ toYaml .Values.affinity | indent 8 }}
{{- end }}
{{- if .Values.tolerations }}
      tolerations:
{{ toYaml .Values.tolerations | indent 6 }}
{{- end }}
      serviceAccountName: {{ template "siriusec.serviceAccountName" . }}
